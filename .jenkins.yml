container: aozora0000/jenkins-ci-php
steps:
    - name: composerインストール
      code: composer update
    - name: PHPUNIT起動
      code: phpunit
notify:
    - service: idobata
      token: $IDOBATA_TOKEN
    - service: chatwork
      token: $CHATWORK_TOKEN
      room_id: $CHATWORK_ROOM_ID